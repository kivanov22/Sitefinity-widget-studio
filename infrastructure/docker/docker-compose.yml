version: "3.9"

services:
  studio:
    build:
      context: ../..
      dockerfile: infrastructure/docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Placeholder: add postgres for widget registry in v0.3
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: widget_studio
  #     POSTGRES_USER: studio
  #     POSTGRES_PASSWORD: studio
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   pgdata:
