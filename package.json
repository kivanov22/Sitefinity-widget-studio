{
  "name": "sitefinity-widget-studio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/studio",
    "build": "npm run build --workspace=apps/studio",
    "lint": "npm run lint --workspace=apps/studio",
    "type-check": "npm run type-check --workspace=apps/studio"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
